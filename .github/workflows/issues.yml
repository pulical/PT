name: Issue workflow

on:
  issues:
    types: [opened, closed, edited, reopened]
  pull_request:
    types: [opened, closed]
  workflow_dispatch: 

  push:
    tags:
      - '*'

    
jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        
      - name: Run tests
        run: |
            ls
            python pytest.py
          # Run your test cases here
          # Exit with non-zero status if tests fail
          
      - name: Get Time
        id: time
        uses: nanzm/get-time-action@v1.1
        with:
          timeZone: 8
          format: 'YYYY-MM-DD-HH-mm-ss'
      - name: Usage
        env:
          TIME: "${{ steps.time.outputs.time }}"
        run: |
          echo $TIME
          
      - name: Remove dashes from timestamp
        run: |
          CLEAN_TIMESTAMP=${{ steps.time.outputs.time }}
          CLEAN_TIMESTAMP=${CLEAN_TIMESTAMP//-/}
          echo "Clean timestamp: $CLEAN_TIMESTAMP"
        id: clean_time
        env:
          CLEAN_TIME: ${{ steps.clean_time.outputs.CLEAN_TIMESTAMP }}
      
      - name: "Build & test"
        run: |
          echo "done!"
      - uses: "nanzm/get-time-action@v1.1"
        with:
          repo_token: "${{ secrets.GIT_SECRET }}"
          automatic_release_tag: "${{ env.CLEAN_TIME }}"
          prerelease: true
          title: "Development Build"
          files: |
            pytest.py
